getElementById doit être utilisé juste calculer des choses dynamiquement en javascript et les afficher

xttp m'en servir juste pour envoyer les modifications faites en front vers la DB et rien d'autres (pas pour reconstruire des pages)


Quand je creer une nouvelle routes ne pas oublier d'aller l'ajouter dans __init__


Pour DB

Ne pas oublier d'importer du côté où il y a la FK le models de l'autre.
Prendre en model la relation jeei.py et specification.py


Enum

Attention pour printer comme un string il faut leur ajouter .name




getElementbyID.innerHTML sert à recuper le code HTML 

getElementbyId().value sert à recuperer la valeur dans le champs <div> valeur ici </div>


un message flash python n'intervient que si il y a rechargement de la page (donc passage via la route/backend)

Attention que les placeholder...meme si je mets des variables JINJA...je dois mettre des " "



Json < 

le probleme que j'ai eu c'est que j'avais changé le constructeur de la DB et meme si la fonction n'avait rien à voir avec ce que je faisais et qu'il ne me 'lindiquait pas...il me disait que erreur...
Je m'en suis apercu en essayent de creer un new JEEI et en remplissant...là il a été explicite